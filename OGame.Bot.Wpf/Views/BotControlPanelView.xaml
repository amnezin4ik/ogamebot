<UserControl x:Class="OGame.Bot.Wpf.Views.BotControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OGame.Bot.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="UserName" 
                       Margin="10" 
                       />
            <TextBox Text="{Binding CurrentUser.UserName, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200"
                     Margin="10"
                     >
            </TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Password" 
                       Margin="10" 
                       />
            <TextBox Text="{Binding CurrentUser.Password, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200"
                     Margin="10"
                     >
            </TextBox>
        </StackPanel>
        <Button Name="RunBot" 
                Content="RunBot"
                Width="100" 
                />
    </StackPanel>
</UserControl>
